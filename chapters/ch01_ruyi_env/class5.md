# Class 5：ruyi device provision 适用边界

## 本讲目标

- 能解释 device provision 的目的、输入、输出和潜在风险。
- 能使用当前 `ruyi` 版本查询设备支持信息，而不是凭板卡名称猜测。
- 能根据设备、镜像、连接方式和数据风险选择自动 provision 或手工准备流程。

## 前置条件

- 已安装并验证 `ruyi`。
- 已完成至少一次 RevyOS 手工烧录和首次启动。
- 已保存重要数据，并能识别课程目标板和存储介质。

## 知识简介

Device provision 是把设备准备成可使用状态的自动化过程，可能包括下载镜像、选择设备变体、写入存储介质或生成操作指引。它的价值是减少重复步骤，但只有在工具明确支持设备、镜像和连接方式时才可靠。自动化不会消除误选磁盘、断电或版本不匹配的风险，因此“能否运行命令”不等于“适合运行命令”。

图 1-5 建议展示决策流程：

```text
查询当前 ruyi 帮助与设备列表
            ↓
设备与变体是否明确受支持？
       ┌────┴────┐
      是          否
      ↓           ↓
核对镜像、介质、  使用官方手工流程
连接和数据风险    并记录不支持原因
      ↓
执行 provision 并验证
```

## 环境准备

| 项目 | 要求 | 检查方式 |
| --- | --- | --- |
| `ruyi` | 已安装且软件源可查询 | `ruyi --version` |
| 设备资料 | 型号、硬件版本、存储方式明确 | 核对丝印和说明 |
| 数据保护 | 重要数据已备份 | 检查备份文件 |
| 连接 | 使用工具要求的 USB、串口或存储设备连接 | 查看当前帮助 |
| 电源 | provision 期间稳定供电 | 核对电源规格 |

## 操作步骤

### 步骤 1：读取当前版本帮助

```bash
ruyi device --help
ruyi device provision --help
```

若当前版本没有这些子命令，记录版本与帮助输出，不自行拼凑参数。这本身就是“当前版本不具备该路径”的有效结论。

### 步骤 2：查询设备与变体

使用帮助中提供的列表或查询命令，记录设备标识、变体、支持的镜像和 provision 方式。板卡商品名相同但硬件版本、内存或存储配置不同，也可能需要不同变体。

### 步骤 3：执行风险检查

在运行任何写入操作前逐项回答：

1. 设备是否在当前软件源中明确列出？
2. 变体是否与实物一致？
3. 写入目标是否唯一且可确认？
4. 重要数据是否已备份？
5. 供电和数据连接是否稳定？
6. 是否已准备串口或其他恢复通道？

任一项不能确认，都应停止自动 provision。

### 步骤 4：选择操作路径

- 明确支持：按当前帮助逐步执行，保留下载、选择和写入日志。
- 未明确支持：使用板卡或 RevyOS 官方手工烧录流程。
- 仅部分支持：只使用已确认安全的查询、下载或生成指引能力，不执行未知写入步骤。

### 步骤 5：完成后验证

验证项目包括设备能启动、系统版本正确、架构正确、网络可用，并记录是否需要额外手工配置。

```bash
cat /etc/os-release
uname -m
ip -br addr
```

## 课堂练习

给出三个场景并作决策：设备完全匹配但有重要数据；设备未列出但名称相似；设备列出且为新空白存储。说明选择自动或手工流程的理由。

## 运行验证

| 验证项 | 预期现象 | 记录 |
| --- | --- | --- |
| 帮助检查 | 能确认当前版本是否提供 provision | 保存输出 |
| 设备查询 | 能得到明确支持信息或明确“不支持”结论 | 记录依据 |
| 风险清单 | 六项均有答案 | 提交清单 |
| 路径选择 | 自动或手工流程理由清楚 | 提交决策记录 |
| 结果验证 | 若执行写入，系统能启动并通过基础检查 | 保存输出 |

## 常见问题

### 把相似设备当成同一设备

商品名、SoC 或外观相似不能证明镜像和写入流程兼容。必须使用工具或官方资料中的精确标识。

### 未确认写入目标

自动化写入仍可能覆盖错误存储设备。目标不唯一时应断开无关设备或改用更可控的流程。

### 帮助示例与本机版本不同

以本机 `--help` 和当前软件源为准，记录版本差异，不盲目尝试旧参数。

## 本讲成果

- 当前 `ruyi` 版本的 device/provision 帮助记录。
- 课程目标板设备支持查询结果。
- provision 风险检查表。
- 自动或手工准备路径的决策记录与验证结果。
