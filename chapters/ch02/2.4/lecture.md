# 2.4 Makefile：规则、变量、clean

> 状态：骨架草稿，待充实。

## 本讲目标

- 能写含目标、依赖、命令的基本 Makefile 规则。
- 能使用变量定义编译器与编译选项。
- 能实现 `make clean` 清理产物。

## 前置条件

- 完成 2.2。

## 知识简介

Makefile 描述源文件如何编译、链接。后续实验与综合项目均基于此模板扩展。

## 操作步骤

（待补充：引用 `chapters/ch02/code/hello/Makefile` 与 `chapters/ch02/code/project-template/Makefile`。）

## 运行验证

| 验证项 | 预期现象 | 记录 |
| --- | --- | --- |
| `make` | 成功构建 | |
| `make clean` | 删除 build 产物 | |

## 常见问题

（待补充：Tab 缩进、路径空格等。）

## 本讲成果

- 可复用的 Makefile 片段。
- 对应实验 2.4。
