# 2.2 第一个 C 程序：编译 → 传输 → 运行

> 状态：骨架草稿，待充实。

## 本讲目标

- 能编写最小 C 程序并用交叉编译器生成可执行文件。
- 能通过 SCP 将二进制传到开发板并运行。
- 能确认输出来自 target 而非 host。

## 前置条件

- 完成 2.1；第一章 SSH/SCP 通道可用。

## 知识简介

课程第一个完整闭环：编辑源码 → 交叉编译 → 传输 → 板端执行。示例代码见 `chapters/ch02/code/hello/`。

## 环境准备

| 项目 | 要求 | 检查方式 |
| --- | --- | --- |
| 交叉编译器 | `CC` 指向 `riscv64-unknown-linux-gnu-gcc` | `echo $CC` 或 Makefile |
| SCP | 可上传到板端家目录 | `scp` 测试 |

## 操作步骤

（待补充：引用 `chapters/ch02/code/hello/` 的构建与部署步骤。）

## 运行验证

| 验证项 | 预期现象 | 记录 |
| --- | --- | --- |
| 板端执行 | 打印 `Hello, RISC-V Linux!` | |

## 常见问题

（待补充：架构不匹配、`Permission denied` 等。）

## 本讲成果

- 板端可运行的 Hello World。
- 对应实验 2.2 的终端记录。
